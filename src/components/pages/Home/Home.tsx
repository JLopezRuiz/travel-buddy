import { Link } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <h1>Results</h1>
            <Link to="/results">Results</Link>
        </div>
    );
};

export default Home;